/*
import React from 'react';

import VideosListItem from './videos_list_item';

const VotingSectionContainer = (props) => {
  console.log(props, 'props');
  const Videos = props.videos.videos.map((video, idx) => {
    return <VideosListItem video={video}
                           key={video.id}
                           clickHandler={props.clickHandler.bind(this, idx)}
                           />
  });

  return (
    <div>
      {Videos}
    </div>
  )

};

export default VotingSectionContainer;*/
